This folder holds source code for binary run files.
Interpreted code like R genrally resides only in the project root directory.
If an rmd (markdown) file is created then the r code will be moved here and the rmd file will reside in the root.
