This folder holds source code for binary (compiled) files.
Interpreted code files like r generally reside only in the project root directory.
If an rmd (markdown) file is created then the r code will instead be archived, and the rmd file will reside in the root.
